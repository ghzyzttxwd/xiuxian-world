const fs=require('fs');
const crypto=require('crypto');
const INPUT='src/game-v32.js',OUTPUT='src/game-v33.js',CATALOG='content/v33-alchemy-materials.cjs',RUNTIME='tools/v33-alchemy-runtime.txt',REPORT='BUILD_V33_ALCHEMY_MATERIALS.json',BUILD='3301';
for(const f of [INPUT,CATALOG,RUNTIME])if(!fs.existsSync(f))throw new Error('V3.3 build missing '+f);
const data=require('../'+CATALOG),materials=data.materials,recipes=data.recipes,namedMaterials=data.namedMaterials;
if(materials.length!==48||namedMaterials.length!==40)throw new Error('V3.3 material counts mismatch');if(recipes.length!==24)throw new Error('V3.3 recipe count mismatch');
let src=fs.readFileSync(INPUT,'utf8');let runtime=fs.readFileSync(RUNTIME,'utf8').trimEnd().replace('__MATERIAL_DATA__',JSON.stringify(materials)).replace('__RECIPE_DATA__',JSON.stringify(recipes));
function must(search,replacement,label){if(!src.includes(search))throw new Error('V3.3 transform did not match: '+label);src=src.replace(search,replacement)}
function replaceBetween(start,end,replacement,label){const a=src.indexOf(start);if(a<0)throw new Error('V3.3 start anchor missing: '+label);const b=src.indexOf(end,a+start.length);if(b<0)throw new Error('V3.3 end anchor missing: '+label);src=src.slice(0,a)+replacement+src.slice(b)}

must("const SAVE_KEY='xiuxian_world_v02'; const OLD_KEY='xiuxian_world_v01'; const VERSION='3.2.0'; const SAVE_SCHEMA_VERSION=29;","const SAVE_KEY='xiuxian_world_v02'; const OLD_KEY='xiuxian_world_v01'; const VERSION='3.3.0'; const SAVE_SCHEMA_VERSION=30;",'version/schema');
must('const CONTENT_STATE_VERSION=3;','const CONTENT_STATE_VERSION=4;','registry version');

const matRegistry=Object.fromEntries(materials.map(m=>[m.id,{id:m.id,name:m.name,qualityId:m.qualityId,kind:m.kind,field:m.legacyField||null,locations:m.locations||[],minRealm:m.minRealm||0,named:!!m.named}]));
replaceBetween('const MATERIAL_REGISTRY=Object.freeze({','\nconst CONSUMABLE_ITEM_META=Object.freeze({',`const MATERIAL_REGISTRY=Object.freeze(${JSON.stringify(matRegistry)});\n`,'material registry');

replaceBetween('function itemRegistry(){','\nfunction artifactRegistry(){',`function itemRegistry(){const out={};for(const row of V32_EQUIPMENT_CATALOG)out[row.itemId]={id:row.itemId,itemId:row.itemId,name:row.name,type:row.kind,slot:row.slot,artifactSlot:row.artifactSlot,artifactId:row.artifactId,qualityId:row.qualityId,tier:row.tier,realmRequirement:realmIdFromIndex(row.unlock),path:row.path,sources:[...row.sources],cost:JSON.parse(JSON.stringify(row.cost)),stats:JSON.parse(JSON.stringify(row.stats)),passive:JSON.parse(JSON.stringify(row.passive||{})),active:row.active?JSON.parse(JSON.stringify(row.active)):null,legacyKey:row.legacyKey||null};for(const r of V33_RECIPE_CATALOG)out[r.itemId]={id:r.itemId,itemId:r.itemId,type:'consumable',kind:'pill',name:r.name,qualityId:r.qualityId,recipeId:r.id,realmRequirement:realmIdFromIndex(r.unlock),sources:[...r.sources],toxicity:r.toxicity,effect:JSON.parse(JSON.stringify(r.effect||{}))};return out}`,'item registry');
replaceBetween('function recipeRegistry(){','\nfunction enemyRegistry(){',`function recipeRegistry(){const out={};for(const r of V33_RECIPE_CATALOG)out[r.id]={id:r.id,recipeId:r.id,name:r.name,type:'alchemy',realmRequirement:realmIdFromIndex(r.unlock),days:r.days||1,qualityId:r.qualityId,outputItemId:r.itemId,sources:[...r.sources],ingredients:JSON.parse(JSON.stringify(r.ingredients||{})),learnCost:JSON.parse(JSON.stringify(r.learnCost||{})),toxicity:r.toxicity,desc:r.desc};return out}`,'recipe registry');
replaceBetween('function rewardEntries(e){','\nfunction dropTableRegistry(){',`function rewardEntries(e){const r=e.reward||{},out=[];const add=(id,v,mode='fixed')=>{if(Array.isArray(v)&&((v[0]||0)!==0||(v[1]||0)!==0))out.push({materialId:id,min:v[0]||0,max:v[1]||0,mode});else if(Number(v)>0)out.push({materialId:id,min:Number(v),max:Number(v),mode})};add('mat-spirit-stone',r.stones);add('mat-spirit-herb',r.herbs);add('mat-core-essence',r.core);add('mat-nascent-essence',r.nascent);add('mat-deification-essence',r.deification);if(!e.nonLethal)out.push({materialId:'mat-rare-material',min:0,max:e.realm>=23?2:e.realm>=15?1:0,mode:'realm-scaled'});return out.concat(v33MaterialDropEntries(e))}`,'named material drop registry');
replaceBetween('function syncStableContentState(){','\nfunction ensureContentStateShape(){',`function syncStableContentState(){if(!state?.player)return false;const p=state.player;p.contentStateVersion=CONTENT_STATE_VERSION;p.realmId=realmIdFromIndex(p.realmIndex);p.regionId=regionIdFromName(p.location);p.manualId=manualIdFromName(p.manual);p.spellProficiencyById={};for(const [name,prof] of Object.entries(p.spells||{})){const id=spellIdFromName(name);if(id)p.spellProficiencyById[id]=Number(prof)||0}if(p.materialInventoryById&&typeof p.materialInventoryById==='object')p.materialCountsById={...p.materialInventoryById};else{p.materialCountsById={};for(const d of Object.values(MATERIAL_REGISTRY))if(d.field)p.materialCountsById[d.id]=Math.max(0,Number(p[d.field])||0)}if(p.pillInventoryById&&typeof p.pillInventoryById==='object'){p.itemCountsById={};for(const r of V33_RECIPE_CATALOG)p.itemCountsById[r.itemId]=Object.values(p.pillInventoryById[r.id]||{}).reduce((a,n)=>a+(Number(n)||0),0)}else{p.itemCountsById={};for(const d of Object.values(CONSUMABLE_ITEM_META))p.itemCountsById[d.id]=Math.max(0,Number(p[d.field])||0)}if(!Array.isArray(p.gearOwnedIds)||!p.gearOwnedIds.length)p.gearOwnedIds=(Array.isArray(p.gearOwned)?p.gearOwned:[]).map(gearItemIdFromKey).filter(Boolean);if(!p.equippedItemIds||typeof p.equippedItemIds!=='object'||Array.isArray(p.equippedItemIds)){const eq=p.equipped||{};p.equippedItemIds={weapon:gearItemIdFromKey(eq.weapon),armor:gearItemIdFromKey(eq.armor),charm:gearItemIdFromKey(eq.charm)}}if(!Array.isArray(p.artifactOwnedIds))p.artifactOwnedIds=(Array.isArray(p.gearOwned)?p.gearOwned:[]).map(artifactIdFromGearKey).filter(Boolean);return true}`,'stable alchemy state');

must(" 29(){ensureV32GearShape()} \n};"," 29(){ensureV32GearShape()},\n 30(){ensureV33AlchemyShape()} \n};",'schema30 migration');
must('state.player.rootIndex=chooseRoot();state.npcs=createNPCs();ensureContentStateShape();ensureV31GrowthShape();ensureV32GearShape();ensureNpcLifeShape();','state.player.rootIndex=chooseRoot();state.npcs=createNPCs();ensureContentStateShape();ensureV31GrowthShape();ensureV32GearShape();ensureV33AlchemyShape();ensureNpcLifeShape();','new state v33');
must("['player.natalArtifactId',p.natalArtifactId===null?'none':p.natalArtifactId],","['player.natalArtifactId',p.natalArtifactId===null?'none':p.natalArtifactId],['player.materialInventoryById',p.materialInventoryById],['player.pillInventoryById',p.pillInventoryById],['player.knownRecipeIds',p.knownRecipeIds],['player.pillToxicity',p.pillToxicity],['player.pillToleranceById',p.pillToleranceById],['player.alchemyBestQualityByRecipe',p.alchemyBestQualityByRecipe],",'schema v33 fields');
must('function save(){try{if(!state)return false;syncV31GrowthState();syncV32GearState();syncStableContentState();syncV32GearState();state.version=VERSION;','function save(){try{if(!state)return false;syncV31GrowthState();syncV32GearState();syncV33AlchemyState();syncStableContentState();syncV32GearState();syncV33AlchemyState();state.version=VERSION;','save v33 sync');

replaceBetween('const ALCHEMY_RECIPES={','\n\nconst V32_EQUIPMENT_CATALOG=',runtime+'\n\nconst V32_EQUIPMENT_CATALOG=','replace alchemy system');

must('function advanceDays(days){for(let i=0;i<days&&!state.flags.dead;i++)tickOneDay();state.player.qi=Math.min(maxQi(),state.player.qi+Math.max(2,Math.floor(maxQi()*.15))*days);save();render()}','function advanceDays(days){for(let i=0;i<days&&!state.flags.dead;i++)tickOneDay();v33DecayAlchemy(days);state.player.qi=Math.min(maxQi(),state.player.qi+Math.max(2,Math.floor(maxQi()*.15))*days);save();render()}','toxicity time decay');
must('function maxHp(){return Math.max(1,Math.floor((realm().maxHp+gearHp())*daoHpMultiplier()*v31ManualHpMultiplier()*v31PassiveHpMultiplier()))} function maxQi(){return Math.max(0,Math.floor((realm().maxQi+gearQi())*daoQiMultiplier()*v31ManualQiMultiplier()*v31PassiveQiMultiplier()))}','function maxHp(){return Math.max(1,Math.floor((realm().maxHp+gearHp())*daoHpMultiplier()*v31ManualHpMultiplier()*v31PassiveHpMultiplier()*v33BuffMultiplier(\'hp\')))} function maxQi(){return Math.max(0,Math.floor((realm().maxQi+gearQi())*daoQiMultiplier()*v31ManualQiMultiplier()*v31PassiveQiMultiplier()*v33BuffMultiplier(\'qi\')))}','pill max stats');
must('return clamp(base+rootBonus+manualBonus+pity-injuryPenalty,req?.kind?.08:.16,req?.kind?.82:.92)','return clamp(base+rootBonus+manualBonus+pity-injuryPenalty+v33BuffValue(\'breakthrough\'),req?.kind?.08:.16,req?.kind?.82:.92)','pill breakthrough');
must('*v31SkillDamageMultiplier(def)*v32EquippedDamageMultiplier(def);','*v31SkillDamageMultiplier(def)*v32EquippedDamageMultiplier(def)*v33BuffMultiplier(\'damage\');','pill skill damage');
must('*daoMeleeMultiplier()));combat.enemyHp-=dmg;','*daoMeleeMultiplier()*v33BuffMultiplier(\'damage\')));combat.enemyHp-=dmg;','pill melee damage');
must('*v32ArtifactIncomingMultiplier()));','*v32ArtifactIncomingMultiplier()*v33BuffMultiplier(\'incoming\')));','pill incoming damage');
must('chance=clamp(.40+windBonus-diff*.08,.12,.78);','chance=clamp(.40+windBonus+v33BuffValue(\'flee\')-diff*.08,.12,.88);','pill flee bonus');
must('realmSuppressionMultiplier(state.player.realmIndex,enemy.realm||0)/hits));','realmSuppressionMultiplier(state.player.realmIndex,enemy.realm||0)*v33BuffMultiplier(\'damage\')/hits));','pill artifact damage');
must('onSectInternalCombatResult(true,e);onGearCombatWin(e);onDaoCombatWin(e);','onSectInternalCombatResult(true,e);onGearCombatWin(e);onV33CombatWin(e);onDaoCombatWin(e);','named combat drops');
must("if(x<danger+(l.eventRate||.08)+.22){startMinorEvent();return}if(x<danger+(l.eventRate||.08)+.38){","if(x<danger+(l.eventRate||.08)+.22){startMinorEvent();return}if(x<danger+(l.eventRate||.08)+.30){const d=gatherV33Material(state.player.location);if(d){addPersonal('【采集】你在'+d.location+'找到 '+d.name+' ×'+d.count+'。','good');save();render();showResult('材料采集',d.name+' ×'+d.count+'\\n该材料已进入命名材料仓库。','good');return}}if(x<danger+(l.eventRate||.08)+.38){",'named exploration gather');
must('window.__TAIXUAN_TEST__={contentRegistrySnapshot,ensureContentStateShape,syncStableContentState,ensureV31GrowthShape,','window.__TAIXUAN_TEST__={contentRegistrySnapshot,ensureContentStateShape,syncStableContentState,ensureV31GrowthShape,ensureV33AlchemyShape,syncV33AlchemyState,v33CatalogSnapshot,v33MaterialCount,v33AddMaterial,gatherV33Material,learnV33Recipe,brewV33Alchemy,useV33Pill,v33BuffValue,v33BuffMultiplier,v33DecayAlchemy,v33AlchemyChance,v33MaterialDropEntries,','test api v33');

fs.writeFileSync(OUTPUT,src,'utf8');const sha=crypto.createHash('sha256').update(src).digest('hex');
const report={status:'PASS',gameplay_version:'3.3.0',build:BUILD,milestone:'alchemy-material-loop',source:OUTPUT,source_sha256:sha,source_bytes:Buffer.byteLength(src),save_schema_version:30,content_registry_version:4,counts:{materials:materials.length,named_materials:namedMaterials.length,recipes:recipes.length,pills:recipes.length,equipment:60,true_artifacts:24,manuals:28,spells:60,enemies:29,regions:12},checks:['40 named materials plus eight legacy compatibility materials','24 alchemy recipes and pill items','recipe acquisition by realm location and cost','alchemy proficiency affects success and quality','four pill quality grades','success and failure consume real materials','pill toxicity and short-term tolerance','toxicity decays with world time','material sources tied to map and enemy drops','named material combat drops','named material exploration gathering','temporary pill buffs affect combat and breakthrough','high-risk burst pill has real downside','high-tier nascent and deification pills','limited lifespan pill','schema29 to schema30 migration','legacy four pills and eight material fields preserved','V3.2 artifact system preserved','future schema31 protection preserved']};
fs.writeFileSync(REPORT,JSON.stringify(report,null,2)+'\n');console.log('V33_BUILD_PASS',JSON.stringify(report));
