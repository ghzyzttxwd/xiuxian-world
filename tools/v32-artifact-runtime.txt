const V32_EQUIPMENT_CATALOG=Object.freeze(__EQUIPMENT_DATA__);
const V32_ITEM_BY_ID=Object.freeze(Object.fromEntries(V32_EQUIPMENT_CATALOG.map(x=>[x.itemId,x])));
const V32_ITEM_BY_LEGACY=Object.freeze(Object.fromEntries(V32_EQUIPMENT_CATALOG.filter(x=>x.legacyKey).map(x=>[x.legacyKey,x])));
const V32_ITEM_BY_ARTIFACT=Object.freeze(Object.fromEntries(V32_EQUIPMENT_CATALOG.filter(x=>x.artifactId).map(x=>[x.artifactId,x])));
const V32_GRADE_META=Object.freeze({normal:{id:'normal',name:'普通',mult:1},fine:{id:'fine',name:'精良',mult:1.08},superior:{id:'superior',name:'上品',mult:1.18},perfect:{id:'perfect',name:'极品',mult:1.30}});
const V32_ARTIFACT_SLOTS=['assault','guard','support','natal'];
let v32GearFilter='available';
function v32EquipmentRow(id){return V32_ITEM_BY_ID[id]||V32_ITEM_BY_ARTIFACT[id]||V32_ITEM_BY_LEGACY[id]||null}
function v32ArtifactRow(id){const r=v32EquipmentRow(id);return r&&r.trueArtifact?r:null}
function v32OwnedRecord(id){const r=v32EquipmentRow(id);return r&&state?.player?.equipmentInventory?.[r.itemId]||null}
function v32NewRecord(row,grade='normal',legacy=false){return {itemId:row.itemId,grade:V32_GRADE_META[grade]?grade:'normal',refinement:0,warmth:0,damaged:false,bound:!!legacy,natal:false,craftedDay:dayNumber(),majorFailures:0}}
function ensureV32GearShape(){if(!state?.player)return false;const p=state.player;if(!p.equipmentInventory||typeof p.equipmentInventory!=='object'||Array.isArray(p.equipmentInventory))p.equipmentInventory={};if(!p.equippedItemIds||typeof p.equippedItemIds!=='object'||Array.isArray(p.equippedItemIds))p.equippedItemIds={weapon:null,armor:null,charm:null};for(const s of ['weapon','armor','charm'])if(!(s in p.equippedItemIds))p.equippedItemIds[s]=null;if(!p.artifactLoadout||typeof p.artifactLoadout!=='object'||Array.isArray(p.artifactLoadout))p.artifactLoadout={assault:null,guard:null,support:null,natal:null};for(const s of V32_ARTIFACT_SLOTS)if(!(s in p.artifactLoadout))p.artifactLoadout[s]=null;if(p.forgingProf==null)p.forgingProf=0;if(p.forgingAttempts==null)p.forgingAttempts=0;if(p.forgingSuccesses==null)p.forgingSuccesses=0;if(p.forgingFailures==null)p.forgingFailures=0;if(p.natalArtifactId===undefined)p.natalArtifactId=null;if(p.artifactWarmSessions==null)p.artifactWarmSessions=0;if(p.artifactRepairs==null)p.artifactRepairs=0;const legacyOwned=Array.isArray(p.gearOwned)?p.gearOwned:[];for(const key of legacyOwned){const row=V32_ITEM_BY_LEGACY[key];if(row&&!p.equipmentInventory[row.itemId])p.equipmentInventory[row.itemId]=v32NewRecord(row,'normal',row.trueArtifact)}for(const id of Array.isArray(p.gearOwnedIds)?p.gearOwnedIds:[]){const row=v32EquipmentRow(id);if(row&&!p.equipmentInventory[row.itemId])p.equipmentInventory[row.itemId]=v32NewRecord(row,'normal',row.trueArtifact)}const oldEq=p.equipped||{};for(const slot of ['weapon','armor','charm']){if(!p.equippedItemIds[slot]&&oldEq[slot]){const row=V32_ITEM_BY_LEGACY[oldEq[slot]];if(row)p.equippedItemIds[slot]=row.itemId}}for(const row of V32_EQUIPMENT_CATALOG.filter(x=>x.trueArtifact&&p.equipmentInventory[x.itemId])){const rec=p.equipmentInventory[row.itemId];if(row.legacyKey)rec.bound=true;if(rec.bound&&!p.artifactLoadout[row.artifactSlot])p.artifactLoadout[row.artifactSlot]=row.itemId}if(p.natalArtifactId&&p.equipmentInventory[p.natalArtifactId]){p.equipmentInventory[p.natalArtifactId].natal=true;p.artifactLoadout.natal=p.natalArtifactId}return true}
function syncV32GearState(){if(!ensureV32GearShape())return false;const p=state.player;p.gearOwnedIds=Object.keys(p.equipmentInventory).filter(id=>!!V32_ITEM_BY_ID[id]);p.artifactOwnedIds=p.gearOwnedIds.map(id=>V32_ITEM_BY_ID[id]).filter(r=>r?.trueArtifact).map(r=>r.artifactId);if(!Array.isArray(p.gearOwned))p.gearOwned=[];for(const id of p.gearOwnedIds){const row=V32_ITEM_BY_ID[id];if(row?.legacyKey&&!p.gearOwned.includes(row.legacyKey))p.gearOwned.push(row.legacyKey)}if(!p.equipped)p.equipped={weapon:null,armor:null,charm:null};for(const slot of ['weapon','armor','charm']){const row=v32EquipmentRow(p.equippedItemIds[slot]);p.equipped[slot]=row?.legacyKey||null}p.natalArtifactId=p.artifactLoadout.natal||p.natalArtifactId||null;return true}
function v32GradeRoll(row){const prof=Number(state.player.forgingProf)||0,x=rand(),high=Math.min(.22,.03+prof/1600+(row.unlock>=15?.02:0));if(x<high*.22)return 'perfect';if(x<high)return 'superior';if(x<Math.min(.62,.25+prof/900))return 'fine';return 'normal'}
function v32RecordScale(row,rec){if(!row||!rec)return 0;let m=V32_GRADE_META[rec.grade]?.mult||1;m*=1+Math.min(9,rec.refinement||0)*.05;m*=1+Math.min(100,rec.warmth||0)*.002;if(rec.damaged)m*=.55;if(rec.natal)m*=1.25;if(row.path&&row.path!=='none'&&row.path===state.player.daoPath)m*=1.08;return m}
function v32EquippedIds(){ensureV32GearShape();const p=state.player,ids=[p.equippedItemIds.weapon,p.equippedItemIds.armor,p.equippedItemIds.charm,...V32_ARTIFACT_SLOTS.map(s=>p.artifactLoadout[s])].filter(Boolean);return [...new Set(ids)].filter(id=>p.equipmentInventory[id]&&V32_ITEM_BY_ID[id])}
function v32EquippedRows(){return v32EquippedIds().map(id=>({row:V32_ITEM_BY_ID[id],rec:state.player.equipmentInventory[id]}))}
function v32ItemStat(id,key){const row=V32_ITEM_BY_ID[id],rec=v32OwnedRecord(id);if(!row||!rec)return 0;return (Number(row.stats?.[key])||0)*v32RecordScale(row,rec)}
function equippedGear(slot){ensureV32GearShape();const id=state.player.equippedItemIds[slot];const row=v32EquipmentRow(id);return row?{...row,...row.stats}:null}
function gearAtk(){return Math.floor(v32EquippedIds().reduce((n,id)=>n+v32ItemStat(id,'atk'),0))}
function gearDef(){return Math.floor(v32EquippedIds().reduce((n,id)=>n+v32ItemStat(id,'def'),0))}
function gearHp(){return Math.floor(v32EquippedIds().reduce((n,id)=>n+v32ItemStat(id,'hp'),0))}
function gearQi(){return Math.floor(v32EquippedIds().reduce((n,id)=>n+v32ItemStat(id,'qi'),0))}
function gearSpellMultiplier(){return 1+v32EquippedIds().reduce((n,id)=>n+v32ItemStat(id,'spell'),0)}
function v32PassiveTotal(key){return v32EquippedRows().reduce((n,x)=>n+(Number(x.row.passive?.[key])||0)*(x.rec.damaged?.5:1),0)}
function v32ArtifactIncomingMultiplier(){return clamp(1+v32PassiveTotal('incoming'),.55,1.25)}
function v32ArtifactHealingMultiplier(){return clamp(1+v32PassiveTotal('healPct'),1,1.55)}
function v32EquippedDamageMultiplier(def=null){let b=v32PassiveTotal('spellPct');for(const {row,rec} of v32EquippedRows()){const p=row.passive||{},scale=rec.damaged?.5:1;if(def?.path&&def.path!=='none'&&row.path===def.path)b+=(p.pathDamage||0)*scale;if(def?.category==='melee')b+=(p.meleePct||0)*scale;if(def?.statusIds?.some(x=>String(x).includes('burn')))b+=(p.burnBoost||0)*scale}return clamp(1+b,.75,1.85)}
function v32ArtifactPowerMultiplier(row,rec){const manual=v31CurrentManualEffects?.()||{};let m=v32RecordScale(row,rec)*(1+(Number(row.passive?.artifactPower)||0)+(Number(manual.artifactDamage)||0));if(row.path&&row.path!=='none'&&row.path===state.player.daoPath)m*=1.12;return m}
function v32CostText(row){const c=row.cost||{},out=[];if(c.materials)out.push('兽材'+c.materials);if(c.stones)out.push('灵石'+c.stones);if(c.rare)out.push('高阶灵材'+c.rare);if(c.relic)out.push('古修残片'+c.relic);if(c.core)out.push('结丹灵髓'+c.core);if(c.nascent)out.push('化婴灵胎'+c.nascent);if(c.deification)out.push('化神道种'+c.deification);return out.join(' · ')||'无'}
function v32CostReady(row,p=state.player){const c=row.cost||{};return p.beastMaterials>=(c.materials||0)&&p.spiritStones>=(c.stones||0)&&(p.rareMaterials||0)>=(c.rare||0)&&(p.relicFragments||0)>=(c.relic||0)&&(p.coreEssence||0)>=(c.core||0)&&(p.nascentEssence||0)>=(c.nascent||0)&&(p.deificationEssence||0)>=(c.deification||0)}
function v32SpendCost(row,p=state.player){const c=row.cost||{};p.beastMaterials-=c.materials||0;p.spiritStones-=c.stones||0;p.rareMaterials-=c.rare||0;p.relicFragments-=c.relic||0;p.coreEssence-=c.core||0;p.nascentEssence-=c.nascent||0;p.deificationEssence-=c.deification||0}
function v32ForgeChance(row){const p=state.player,prof=Number(p.forgingProf)||0,tierPenalty=row.trueArtifact?.10:row.unlock>=15?.06:0,realmEdge=Math.max(0,p.realmIndex-row.unlock)*.006,locBonus=row.sources.includes('赤霞谷')&&p.location==='赤霞谷'?.03:row.sources.includes('落星矿脉')&&p.location==='落星矿脉'?.03:0;return clamp(.76+Math.min(.16,prof/900)+realmEdge+locBonus-tierPenalty,.38,.97)}
function forgeV32Item(id,forced=null,forcedGrade=null){if(state?.flags?.dead)return 'dead';ensureV32GearShape();const row=v32EquipmentRow(id),p=state.player;if(!row)return 'missing';if(p.equipmentInventory[row.itemId])return 'owned';const force=forced===true||forced==='success';if(!force){if(p.realmIndex<row.unlock)return 'realm';if(!(row.sources||[]).includes(p.location))return 'location';if(!v32CostReady(row,p))return 'cost';v32SpendCost(row,p);p.forgingAttempts++;advanceDays(row.days||2);if(state.flags.dead)return 'dead'}else p.forgingAttempts++;const success=force?true:forced==='fail'?false:rand()<v32ForgeChance(row);if(!success){p.forgingFailures++;p.forgingProf+=row.trueArtifact?8:4;addPersonal('【炼器失败】'+row.name+'在成形前灵纹崩散，本次材料已经损耗。','bad');save();render();return 'failed'}const grade=forcedGrade&&V32_GRADE_META[forcedGrade]?forcedGrade:v32GradeRoll(row);p.equipmentInventory[row.itemId]=v32NewRecord(row,grade,false);p.forgingSuccesses++;p.forgingProf+=row.trueArtifact?18:10;if(row.legacyKey&&!p.gearOwned.includes(row.legacyKey))p.gearOwned.push(row.legacyKey);if(!row.trueArtifact)p.equippedItemIds[row.slot]=row.itemId;syncV32GearState();addPersonal('【炼器成功】你炼成'+V32_GRADE_META[grade].name+'【'+row.name+'】'+(row.trueArtifact?'，尚需认主后方可收入法宝位':'，并立即装备')+'。','major');save();render();return grade}
function forgeGear(id){const r=v32EquipmentRow(id);const result=forgeV32Item(r?.itemId||id);if(result==='realm')return showResult('境界不足','当前境界不足以炼制该器物。','bad');if(result==='location')return showResult('炼器地脉不合','需前往该器物标注的炼制来源地。','bad');if(result==='cost')return showResult('材料不足','所需：'+v32CostText(r),'bad');if(result==='owned')return showResult('已经拥有','这件器物已经在你的收藏中。');if(V32_GRADE_META[result])showResult('炼器成功',V32_GRADE_META[result].name+'【'+r.name+'】已入手。','good');else if(result==='failed')showResult('炼器失败','本次材料已经消耗，炼器熟练度仍有增长。','bad')}
function equipGear(id){ensureV32GearShape();const row=v32EquipmentRow(id);if(!row||row.trueArtifact||!v32OwnedRecord(row.itemId))return false;state.player.equippedItemIds[row.slot]=row.itemId;syncV32GearState();state.player.hp=Math.min(state.player.hp,maxHp());state.player.qi=Math.min(state.player.qi,maxQi());save();render();return true}
function bindV32Artifact(id,force=false){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId),p=state.player;if(!row||!rec)return 'missing';if(rec.bound)return 'bound';if(!force){if(p.realmIndex<10)return 'realm';if((p.insight||0)<1||p.spiritStones<8)return 'cost';p.insight--;p.spiritStones-=8;advanceDays(1);if(state.flags.dead)return 'dead'}rec.bound=true;addPersonal('你以神识在【'+row.name+'】中留下烙印，完成认主。','major');save();render();return 'ok'}
function equipV32Artifact(id,slot=null){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId);if(!row||!rec||!rec.bound||rec.damaged)return false;const s=slot||row.artifactSlot;if(!V32_ARTIFACT_SLOTS.includes(s)||s==='natal')return false;for(const k of ['assault','guard','support'])if(state.player.artifactLoadout[k]===row.itemId)state.player.artifactLoadout[k]=null;state.player.artifactLoadout[s]=row.itemId;save();render();return true}
function v32RefineCost(row,rec){const l=rec.refinement||0;return {stones:18+row.unlock*2+l*12,rare:1+Math.floor(l/3),relic:l>=5?1+Math.floor((l-5)/2):0}}
function refineV32Artifact(id,forced=null){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId),p=state.player;if(!row||!rec)return 'missing';if(rec.damaged)return 'damaged';if((rec.refinement||0)>=9)return 'max';const c=v32RefineCost(row,rec),force=forced===true||forced==='success'||forced==='damage';if(!force){if(p.spiritStones<c.stones||(p.rareMaterials||0)<c.rare||(p.relicFragments||0)<c.relic)return 'cost';p.spiritStones-=c.stones;p.rareMaterials-=c.rare;p.relicFragments-=c.relic;advanceDays(2);if(state.flags.dead)return 'dead'}const chance=clamp(.88-(rec.refinement||0)*.055+Math.min(.10,(p.forgingProf||0)/1600),.38,.94),ok=forced==='success'||forced===true?true:forced==='damage'?false:rand()<chance;if(ok){rec.refinement++;p.forgingProf+=12;addPersonal('【祭炼成功】'+row.name+'祭炼至 '+rec.refinement+' 层，器物与你的灵力愈发契合。','major');save();render();return 'success'}rec.majorFailures=(rec.majorFailures||0)+1;p.forgingProf+=5;const severe=forced==='damage'||rand()<.18+(rec.refinement||0)*.025;if(severe){rec.damaged=true;addPersonal('【祭炼重挫】'+row.name+'灵纹受损，必须修复后才能继续祭炼或发动主动能力。','bad');save();render();return 'damage'}addPersonal('【祭炼失败】'+row.name+'本次祭炼未能更进一步，但主体没有损坏。','bad');save();render();return 'failed'}
function repairV32Artifact(id,force=false){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId),p=state.player;if(!row||!rec)return 'missing';if(!rec.damaged)return 'healthy';const stones=35+row.unlock*3,rare=2+Math.floor((rec.refinement||0)/3);if(!force){if(p.spiritStones<stones||(p.rareMaterials||0)<rare)return 'cost';p.spiritStones-=stones;p.rareMaterials-=rare;advanceDays(3);if(state.flags.dead)return 'dead'}rec.damaged=false;p.artifactRepairs=(p.artifactRepairs||0)+1;p.forgingProf+=8;addPersonal('你重新梳理【'+row.name+'】受损灵纹，法宝恢复完整。','major');save();render();return 'ok'}
function warmV32Artifact(id,force=false){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId),p=state.player;if(!row||!rec)return 'missing';if(!rec.bound)return 'unbound';if(rec.damaged)return 'damaged';if((rec.warmth||0)>=100)return 'max';const stones=8+Math.floor(row.unlock/3);if(!force){if(p.spiritStones<stones)return 'cost';p.spiritStones-=stones;advanceDays(3);if(state.flags.dead)return 'dead'}const gain=force?10:rint(6,12);rec.warmth=Math.min(100,(rec.warmth||0)+gain);p.artifactWarmSessions=(p.artifactWarmSessions||0)+1;addPersonal('你以自身灵力温养【'+row.name+'】三日，温养度提升至 '+rec.warmth+'/100。','good');save();render();return gain}
function makeNatalV32Artifact(id,force=false){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId),p=state.player;if(!row||!rec)return 'missing';if(rec.natal)return 'natal';if(p.natalArtifactId&&p.natalArtifactId!==row.itemId)return 'occupied';if(!rec.bound)return 'unbound';if(!force){if(p.realmIndex<15)return 'realm';if((rec.refinement||0)<3||(rec.warmth||0)<20)return 'foundation';if((p.insight||0)<5||(p.rareMaterials||0)<3||p.spiritStones<50)return 'cost';p.insight-=5;p.rareMaterials-=3;p.spiritStones-=50;advanceDays(7);if(state.flags.dead)return 'dead'}else{rec.refinement=Math.max(3,rec.refinement||0);rec.warmth=Math.max(20,rec.warmth||0);rec.bound=true}rec.natal=true;p.natalArtifactId=row.itemId;p.artifactLoadout.natal=row.itemId;addPersonal('【本命法宝】你将【'+row.name+'】纳入丹田与神魂长期温养，从此它与此世根基绑定。','major');save();render();return 'ok'}
function v32ArtifactCooldownRemaining(itemId){if(!combat)return 0;combat.artifactCooldowns||(combat.artifactCooldowns={});return Math.max(0,Number(combat.artifactCooldowns[itemId])||0)}
function v32SetArtifactCooldown(itemId,n){if(!combat)return 0;combat.artifactCooldowns||(combat.artifactCooldowns={});combat.artifactCooldowns[itemId]=Math.max(0,Number(n)||0);return combat.artifactCooldowns[itemId]}
function v32TickArtifactCooldowns(){if(!combat)return;combat.artifactCooldowns||(combat.artifactCooldowns={});for(const id of Object.keys(combat.artifactCooldowns)){combat.artifactCooldowns[id]=Math.max(0,(Number(combat.artifactCooldowns[id])||0)-1);if(!combat.artifactCooldowns[id])delete combat.artifactCooldowns[id]}}
function v32LoadedArtifactRows(){ensureV32GearShape();const ids=V32_ARTIFACT_SLOTS.map(s=>state.player.artifactLoadout[s]).filter(Boolean);return [...new Set(ids)].map(id=>({row:V32_ITEM_BY_ID[id],rec:v32OwnedRecord(id)})).filter(x=>x.row?.trueArtifact&&x.rec?.bound)}
function v32ArtifactCombatButtons(){if(!combat)return '';return v32LoadedArtifactRows().filter(x=>x.row.active).map(({row,rec})=>{const cd=v32ArtifactCooldownRemaining(row.itemId),disabled=rec.damaged||cd>0;return '<button data-combat="artifact:'+row.itemId+'" '+(disabled?'disabled':'')+'>法宝·'+esc(row.name)+(rec.damaged?' · 受损':cd?' · 冷却'+cd:'')+'</button>'}).join('')}
function v32ArtifactDamage(row,rec,enemy,active){const scale=v32ArtifactPowerMultiplier(row,rec),base=22+row.unlock*11+(row.stats.atk||0)*.7+(row.stats.spell||0)*360+(rec.refinement||0)*12+(rec.warmth||0)*.8,hits=Math.max(1,Number(active.hits)||1);let total=0;for(let i=0;i<hits;i++)total+=Math.max(1,Math.floor(base*(Number(active.power)||1)*scale*realmSuppressionMultiplier(state.player.realmIndex,enemy.realm||0)/hits));if(active.executeBelow&&combat.enemyHp/enemy.hp<=active.executeBelow)total=Math.floor(total*(active.executeMult||1.5));return total}
function useV32ArtifactActive(id,enemy){ensureV32GearShape();const row=v32ArtifactRow(id),rec=row&&v32OwnedRecord(row.itemId),active=row?.active;if(!combat||!row||!rec||!active)return false;if(!v32LoadedArtifactRows().some(x=>x.row.itemId===row.itemId)){combat.logs.push('该法宝没有装入法宝位。');return false}if(!rec.bound){combat.logs.push(row.name+'尚未认主。');return false}if(rec.damaged){combat.logs.push(row.name+'灵纹受损，无法发动。');return false}const cd=v32ArtifactCooldownRemaining(row.itemId);if(cd>0){combat.logs.push(row.name+'尚在冷却，还需 '+cd+' 回合。');return false}let damage=0;if(['strike','multi','burn','execute','control','leech'].includes(active.type)){damage=v32ArtifactDamage(row,rec,enemy,active);combat.enemyHp-=damage}if(active.shieldPct){const gain=Math.max(1,Math.floor(maxHp()*active.shieldPct*v32ArtifactPowerMultiplier(row,rec)));combat.v31Shield=(combat.v31Shield||0)+gain;combat.logs.push(row.name+'展开护持，护盾 +'+gain+'。')}if(active.healPct){const gain=Math.max(1,Math.floor(maxHp()*active.healPct*v32ArtifactHealingMultiplier()*v32ArtifactPowerMultiplier(row,rec))),before=combat.playerHp;combat.playerHp=Math.min(maxHp(),combat.playerHp+gain);combat.logs.push(row.name+'回养气血 '+(combat.playerHp-before)+'。')}if(active.qiPct){const gain=Math.max(1,Math.floor(maxQi()*active.qiPct*v32ArtifactPowerMultiplier(row,rec))),before=combat.playerQi;combat.playerQi=Math.min(maxQi(),combat.playerQi+gain);combat.logs.push(row.name+'回补灵力 '+(combat.playerQi-before)+'。')}if(active.control)combat.v31Control=Math.max(combat.v31Control||0,active.control);if(active.counter)combat.v31Counter=Math.max(combat.v31Counter||0,active.counter);if(active.burn)combat.burn=Math.max(combat.burn||0,active.burn);if(active.type==='domain'){combat.v31Control=Math.max(combat.v31Control||0,active.control||1)}if(active.type==='cleanse'){combat.weaken=0;combat.soulSeal=0;combat.statuses={};combat.logs.push(row.name+'扫去缠身的负面灵机。')}if(active.type==='leech'&&damage>0){const gain=Math.max(1,Math.floor(damage*(active.drain||.2))),before=combat.playerHp;combat.playerHp=Math.min(maxHp(),combat.playerHp+gain);combat.logs.push(row.name+'夺取生机，气血 +'+(combat.playerHp-before)+'。')}let cooldown=Math.max(1,Number(active.cooldown)||4);if(rec.natal)cooldown=Math.max(1,cooldown-1);if(v32PassiveTotal('cooldownEdge')>=.08)cooldown=Math.max(1,cooldown-1);v32SetArtifactCooldown(row.itemId,cooldown+1);combat.logs.push('你催动法宝【'+row.name+'】'+(damage?'，造成 '+damage+' 点伤害。':'。'));return true}
function v32AfterCombatRound(){v32TickArtifactCooldowns()}
function v32RecordSummary(row,rec){if(!rec)return '';const tags=[V32_GRADE_META[rec.grade]?.name||'普通','祭炼'+(rec.refinement||0)+'层','温养'+(rec.warmth||0)];if(rec.bound)tags.push('已认主');if(rec.natal)tags.push('本命');if(rec.damaged)tags.push('受损');return tags.join(' · ')}
function v32ArtifactSlotName(s){return {assault:'攻伐',guard:'护身',support:'辅助',natal:'本命'}[s]||s}
function v32CatalogSnapshot(){return {equipment:V32_EQUIPMENT_CATALOG.length,artifacts:V32_EQUIPMENT_CATALOG.filter(x=>x.trueArtifact).length,activeArtifacts:V32_EQUIPMENT_CATALOG.filter(x=>x.trueArtifact&&x.active).length,buildPassives:V32_EQUIPMENT_CATALOG.filter(x=>x.trueArtifact&&x.buildPassive).length,slots:{base:['weapon','armor','charm'],artifact:[...V32_ARTIFACT_SLOTS]},tiers:[...new Set(V32_EQUIPMENT_CATALOG.map(x=>x.tier))]}}
function renderGear(){const info=$('gearInfo'),actions=$('gearActions');if(!info||!actions)return;ensureV32GearShape();const p=state.player,owned=Object.keys(p.equipmentInventory).length,arts=Object.keys(p.equipmentInventory).filter(id=>V32_ITEM_BY_ID[id]?.trueArtifact).length,natal=p.natalArtifactId&&V32_ITEM_BY_ID[p.natalArtifactId];info.innerHTML='<div class="kv"><span>炼器熟练度</span><b>'+p.forgingProf+'</b></div><div class="kv"><span>收藏</span><b>'+owned+' / 60 · 真正法宝 '+arts+' / 24</b></div><div class="kv"><span>炼器成功 / 失败</span><b>'+p.forgingSuccesses+' / '+p.forgingFailures+'</b></div><div class="kv"><span>本命法宝</span><b>'+esc(natal?.name||'尚未建立')+'</b></div><div class="section-tip">基础装备：武器 / 护甲 / 佩饰。法宝位：攻伐 / 护身 / 辅助 / 本命。法宝需先认主；祭炼重大失败会受损，但不会按每次攻击扣耐久。</div><div class="actions-grid"><button data-v32-filter="available">当前可炼</button><button data-v32-filter="owned">已拥有</button><button data-v32-filter="weapon">武器</button><button data-v32-filter="armor">护甲</button><button data-v32-filter="charm">佩饰</button><button data-v32-filter="artifact">法宝</button></div>';let rows=V32_EQUIPMENT_CATALOG.filter(r=>{const has=!!p.equipmentInventory[r.itemId];if(v32GearFilter==='owned')return has;if(v32GearFilter==='weapon'||v32GearFilter==='armor'||v32GearFilter==='charm')return r.slot===v32GearFilter;if(v32GearFilter==='artifact')return r.trueArtifact;return has||(p.realmIndex>=r.unlock&&(r.sources||[]).includes(p.location))});actions.innerHTML=rows.map(row=>{const rec=p.equipmentInventory[row.itemId],owned=!!rec,where=(row.sources||[]).join(' / '),path=row.path&&row.path!=='none'?(DAO_PATHS[row.path]?.name||row.path):'通用';let buttons='';if(!owned)buttons='<button data-v32-forge="'+row.itemId+'">炼制 · '+esc(v32CostText(row))+'</button>';else if(!row.trueArtifact)buttons='<button data-v32-equip="'+row.itemId+'">'+(p.equippedItemIds[row.slot]===row.itemId?'已装备':'装备')+'</button>';else{if(rec.damaged)buttons+='<button data-v32-repair="'+row.itemId+'">修复法宝</button>';if(!rec.bound)buttons+='<button data-v32-bind="'+row.itemId+'">认主 · 悟道1 / 灵石8</button>';if(rec.bound&&!rec.damaged){buttons+='<button data-v32-artifact="'+row.itemId+'">装入'+v32ArtifactSlotName(row.artifactSlot)+'位</button><button data-v32-refine="'+row.itemId+'">祭炼</button><button data-v32-warm="'+row.itemId+'">温养 · 3日</button>';if(!p.natalArtifactId||p.natalArtifactId===row.itemId)buttons+='<button data-v32-natal="'+row.itemId+'">'+(rec.natal?'已为本命':'本命化')+'</button>'}}return '<div class="skill-card"><div class="skill-head"><b>【'+esc(row.name)+'】</b><span>'+esc(row.trueArtifact?v32ArtifactSlotName(row.artifactSlot)+'法宝':row.slot)+' · '+esc(row.tier)+'</span></div><div class="skill-desc">境界：'+REALMS[Math.min(row.unlock,REALMS.length-1)].name+' · 道途：'+esc(path)+'<br>来源：'+esc(where)+'<br>'+(owned?esc(v32RecordSummary(row,rec)):'炼制：'+esc(v32CostText(row)))+(row.active?'<br>主动法宝能力：'+esc(row.active.type)+' · 冷却 '+row.active.cooldown+'回合':'')+'</div><div class="event-actions">'+buttons+'</div></div>'}).join('')||'<div class="section-tip">当前筛选下没有可显示的器物。</div>';info.querySelectorAll('[data-v32-filter]').forEach(b=>b.onclick=()=>{v32GearFilter=b.dataset.v32Filter;renderGear()});actions.querySelectorAll('[data-v32-forge]').forEach(b=>b.onclick=()=>forgeGear(b.dataset.v32Forge));actions.querySelectorAll('[data-v32-equip]').forEach(b=>b.onclick=()=>{equipGear(b.dataset.v32Equip);showResult('装备完成','已经切换基础装备。','good')});actions.querySelectorAll('[data-v32-bind]').forEach(b=>b.onclick=()=>{const x=bindV32Artifact(b.dataset.v32Bind);showResult('法宝认主',x==='ok'?'认主完成。':'暂时无法认主。',x==='ok'?'good':'bad')});actions.querySelectorAll('[data-v32-artifact]').forEach(b=>b.onclick=()=>{const ok=equipV32Artifact(b.dataset.v32Artifact);showResult('法宝位',ok?'法宝已经装入对应槽位。':'当前无法装备。',ok?'good':'bad')});actions.querySelectorAll('[data-v32-refine]').forEach(b=>b.onclick=()=>{const x=refineV32Artifact(b.dataset.v32Refine);showResult('祭炼法宝',x==='success'?'祭炼成功。':x==='damage'?'重大失败，法宝受损。':x==='failed'?'祭炼失败，但法宝未损。':'当前无法祭炼。',x==='success'?'good':'bad')});actions.querySelectorAll('[data-v32-repair]').forEach(b=>b.onclick=()=>{const x=repairV32Artifact(b.dataset.v32Repair);showResult('修复法宝',x==='ok'?'法宝已修复。':'材料或状态不满足。',x==='ok'?'good':'bad')});actions.querySelectorAll('[data-v32-warm]').forEach(b=>b.onclick=()=>{const x=warmV32Artifact(b.dataset.v32Warm);showResult('温养法宝',Number.isFinite(x)?'温养度 +'+x:'当前无法温养。',Number.isFinite(x)?'good':'bad')});actions.querySelectorAll('[data-v32-natal]').forEach(b=>b.onclick=()=>{const x=makeNatalV32Artifact(b.dataset.v32Natal);showResult('本命法宝',x==='ok'||x==='natal'?'本命关系已经建立。':'本命化条件尚未满足。',x==='ok'||x==='natal'?'good':'bad')})}
