const V36_HIGH_REGIONS=Object.freeze({
 '天渊城':{tier:1,minRealm:23,days:3},
 '空冥裂谷':{tier:2,minRealm:25,days:4},
 '虚空裂隙':{tier:3,minRealm:26,days:5},
 '上古断界台':{tier:4,minRealm:28,days:6}
});
function ensureV36VoidShape(){
 if(!state?.player||!state?.world)return false;const p=state.player,w=state.world;
 if(p.v36VoidVersion==null)p.v36VoidVersion=1;
 if(p.v36SpaceInsight==null)p.v36SpaceInsight=0;
 if(p.v36VoidBacklash==null)p.v36VoidBacklash=0;
 if(p.v36RefiningFailures==null)p.v36RefiningFailures=0;
 if(p.v36SpaceActions==null)p.v36SpaceActions=0;
 if(p.v36SpaceSkillUses==null)p.v36SpaceSkillUses=0;
 if(p.v36RiftWins==null)p.v36RiftWins=0;
 if(p.voidEssence==null)p.voidEssence=0;
 if(w.v36VoidEvents==null)w.v36VoidEvents=0;
 if(w.v36LastVoidEventDay==null)w.v36LastVoidEventDay=0;
 return true
}
function syncV36VoidState(){ensureV36VoidShape();syncV33AlchemyState();return true}
function v36MajorReady(req){if(!req||req.kind!=='炼虚破界')return true;ensureV36VoidShape();return v33MaterialCount('mat-v36-void-essence')>=(req.voidEssence||0)&&(state.player.v36SpaceInsight||0)>=(req.spaceInsight||0)}
function v36BreakthroughBonus(req){if(!req||req.kind!=='炼虚破界')return 0;ensureV36VoidShape();return Math.min(.10,Math.max(0,(state.player.v36SpaceInsight-(req.spaceInsight||0))*.004))}
function v36MinorBreakthroughDays(index){const i=Number(index)||0;return i===26?12:i===27?18:i===28?24:3}
function v36TravelGate(route,to=null){ensureV36VoidShape();if(!route)return {ok:false,reason:'missing'};const p=state.player,min=Number(route.minRealm)||0,space=Number(route.spaceInsight)||0;if(p.realmIndex<min)return {ok:false,reason:'realm',needRealm:min,to:to||null};if((p.v36SpaceInsight||0)<space)return {ok:false,reason:'space',needSpace:space,to:to||null};return {ok:true,reason:'ok'}}
function v36NamedCostReady(cost){const named=cost?.named||{};return Object.entries(named).every(([id,n])=>v33MaterialCount(id)>=Number(n||0))}
function v36SpendNamedCost(cost){for(const [id,n] of Object.entries(cost?.named||{}))v33AddMaterial(id,-Number(n||0));return true}
function v36NamedCostText(cost){return Object.entries(cost?.named||{}).map(([id,n])=>(V33_MATERIAL_BY_ID[id]?.name||id)+n).join(' · ')}
function v36ContemplateSpace(force=false){
 ensureV36VoidShape();const p=state.player,meta=V36_HIGH_REGIONS[p.location];if(!meta)return {ok:false,reason:'location'};if(p.realmIndex<meta.minRealm&&!force)return {ok:false,reason:'realm'};const days=meta.days;if(!force){advanceDays(days);if(state.flags.dead)return {ok:false,reason:'dead'}}
 const raw=force?Math.max(2,meta.tier):1+rint(0,meta.tier),risk=clamp(.22+meta.tier*.045-Math.max(0,p.realmIndex-23)*.018,.06,.38);let gain=raw,backlash=false;
 if(!force&&rand()<risk){backlash=true;gain=Math.max(1,Math.floor(raw*.5));p.v36VoidBacklash++;p.injury=clamp((p.injury||0)+1,0,3);p.hp=Math.max(1,Math.floor(maxHp()*.52));addPersonal('【虚空反噬】你参悟空间褶皱时神识失衡，坐标错位撕伤经脉。','bad')}
 p.v36SpaceInsight=Math.min(120,(p.v36SpaceInsight||0)+gain);p.v36SpaceActions++;if(!force)wVoidEvent();addPersonal('【空间感悟】你在'+p.location+'观测空间褶皱，空间感悟 +'+gain+'（当前 '+p.v36SpaceInsight+'）。',backlash?'bad':'major');save();render();return {ok:true,gain,backlash,spaceInsight:p.v36SpaceInsight}
}
function wVoidEvent(){ensureV36VoidShape();const w=state.world;w.v36VoidEvents++;w.v36LastVoidEventDay=dayNumber();return w.v36VoidEvents}
function v36CraftVoidEssence(force=false){
 ensureV36VoidShape();const p=state.player,allowed=['空冥裂谷','虚空裂隙','上古断界台'];if(!allowed.includes(p.location)&&!force)return {ok:false,reason:'location'};if(p.realmIndex<25&&!force)return {ok:false,reason:'realm'};if((p.v36SpaceInsight||0)<12&&!force)return {ok:false,reason:'space'};const cost={'mat-v36-space-crystal':2,'mat-v36-void-sand':2,'mat-v36-rift-silk':1};if(!force){if(!Object.entries(cost).every(([id,n])=>v33MaterialCount(id)>=n)||(p.deificationEssence||0)<1)return {ok:false,reason:'cost'};for(const [id,n] of Object.entries(cost))v33AddMaterial(id,-n);p.deificationEssence--;advanceDays(5);if(state.flags.dead)return {ok:false,reason:'dead'}}
 v33AddMaterial('mat-v36-void-essence',1);addPersonal('【炼虚真髓】你借空间褶皱将空冥晶、虚空砂与裂界丝凝成一份炼虚真髓。','major');save();render();return {ok:true,count:v33MaterialCount('mat-v36-void-essence')}
}
function v36AttemptRefiningBreakthrough(force=null){
 ensureV36VoidShape();const p=state.player;if(p.realmIndex!==25)return {ok:false,reason:'realm'};const need=realm().need;if(p.progress<need)return {ok:false,reason:'progress'};const req=majorBreakthroughRequirements();if(!req||req.kind!=='炼虚破界')return {ok:false,reason:'requirements'};if(!majorBreakthroughReady(req))return {ok:false,reason:'cost',missing:v36RefiningMissing(req)};
 const oldRealm=realm().name,chance=breakthroughChance();p.breakthroughAttempts++;p.deificationEssence-=req.deification||0;p.insight-=req.insight||0;v33AddMaterial('mat-v36-void-essence',-(req.voidEssence||0));if(force==null){advanceDays(req.days);if(state.flags.dead)return {ok:false,reason:'dead'}}
 const success=force==='success'?true:force==='fail'?false:rand()<chance;if(success){p.progress=0;p.realmIndex=26;p.breakthroughPity=0;p.hp=maxHp();p.qi=maxQi();const lifeGain=applyRealmLifespanMilestone();p.reputation+=300;addWorld(p.name+'撕开自身元神与虚空之间的界限，正式踏入炼虚初期。','major');addPersonal('【炼虚】你以空间感悟固定自身坐标，跨过化神与炼虚之间的界限。'+(lifeGain?'寿元上限提升 '+lifeGain+' 年。':''),'major');save();render();if(force==null)showResult('炼虚成功',oldRealm+' → 炼虚初期\n空间感悟 '+p.v36SpaceInsight+'\n寿元上限 '+p.lifespan,'good');return {ok:true,outcome:'success',chance,realmIndex:p.realmIndex}}
 const oldProgress=p.progress,ratio=force==='fail'?.35:rint(28,42)/100;p.progress=Math.max(0,Math.floor(need*ratio));p.breakthroughPity=Math.min(66,(p.breakthroughPity||0)+req.pity);p.v36RefiningFailures++;p.v36VoidBacklash++;p.injury=3;p.hp=Math.max(1,Math.floor(maxHp()*.05));p.v36SpaceInsight=Math.max(0,(p.v36SpaceInsight||0)-3);const loss=force==='fail'?Math.floor((req.lifeLoss[0]+req.lifeLoss[1])/2):rint(req.lifeLoss[0],req.lifeLoss[1]),before=p.lifespan;p.lifespan=Math.max(age()+5,p.lifespan-loss);let artifactDamaged=false;if(p.natalArtifactId&&p.equipmentInventory?.[p.natalArtifactId]&&(force==='fail'?false:rand()<.35)){p.equipmentInventory[p.natalArtifactId].damaged=true;artifactDamaged=true}const lost=Math.max(0,oldProgress-p.progress),actualLife=Math.max(0,before-p.lifespan);addPersonal('【炼虚失败】空间坐标在最后一刻崩散，元神与肉身同时遭受虚空反噬。','bad');save();render();if(force==null)showResult('炼虚破界失败','修为 -'+lost+'\n空间感悟 -3\n伤势：'+injuryLabel()+'\n寿元 -'+actualLife+(artifactDamaged?'\n本命法宝受损':''),'bad');return {ok:true,outcome:'failure',chance,lost,lifeLoss:actualLife,artifactDamaged,spaceInsight:p.v36SpaceInsight}
}
function v36RefiningMissing(req){const p=state.player,out=[];if((p.injury||0)>0)out.push('必须无伤');if((p.deificationEssence||0)<(req.deification||0))out.push('化神道种 '+req.deification);if((p.insight||0)<(req.insight||0))out.push('悟道点 '+req.insight);if(v33MaterialCount('mat-v36-void-essence')<(req.voidEssence||0))out.push('炼虚真髓 '+req.voidEssence);if((p.v36SpaceInsight||0)<(req.spaceInsight||0))out.push('空间感悟 '+req.spaceInsight);return out}
function v36SpaceDamageMultiplier(effect={}){const rend=Math.max(0,Number(effect.spaceRend)||0);if(!rend)return 1;ensureV36VoidShape();return 1+rend*(1+Math.min(.5,(state.player.v36SpaceInsight||0)/120))}
function v36AfterSpaceSkill(id,row,effect,damage){if(!combat||!effect)return;if(effect.spaceShift){combat.v36SpaceShift=Math.max(combat.v36SpaceShift||0,Number(effect.spaceShift)||1);state.player.v36SpaceSkillUses++;combat.logs.push('【空间挪移】你的坐标短暂错位，脱身与闪避更稳。')}if(effect.spaceLock){combat.v36SpaceLock=Math.max(combat.v36SpaceLock||0,Number(effect.spaceLock)||1);state.player.v36SpaceSkillUses++;combat.logs.push('【空间禁锁】周围位移自由被压制。')}if(effect.voidPhase){combat.v36VoidPhase=Math.max(combat.v36VoidPhase||0,Number(effect.voidPhase)||1);state.player.v36SpaceSkillUses++;combat.logs.push('【虚化】你短暂淡出当前界面，下一次受击将被大幅削弱。')}}
function v36EnemySpaceLocked(){return !!(combat&&(combat.v36SpaceLock||0)>0)}
function v36IncomingMultiplier(){if(!combat)return 1;if((combat.v36VoidPhase||0)>0){combat.v36VoidPhase--;combat.logs.push('【太虚化空】本次来袭被空间错位削弱。');return .38}return 1}
function v36SpaceEscapeBonus(){return combat&&(combat.v36SpaceShift||0)>0?.20:0}
function v36AfterCombatRound(){if(!combat)return;if((combat.v36SpaceLock||0)>0)combat.v36SpaceLock--;if((combat.v36SpaceShift||0)>0)combat.v36SpaceShift--}
function onV36CombatWin(e){ensureV36VoidShape();if(!e||e.nonLethal||!String(e.id||'').startsWith('enemy-v36-'))return null;const p=state.player;p.v36RiftWins++;let insight=0;if(p.v36RiftWins%3===0){insight=1;p.v36SpaceInsight=Math.min(120,(p.v36SpaceInsight||0)+1);addPersonal('【虚空实战】连续与高阶空间生灵厮杀后，你对空间错位多了一分理解。空间感悟 +1。','major')}return {riftWins:p.v36RiftWins,insight}}
function v36CatalogSnapshot(){return {realms:REALMS.length,regions:Object.keys(LOCATIONS).length,routes:TRAVEL_ROUTES.length,materials:V33_MATERIAL_CATALOG.length,recipes:V33_RECIPE_CATALOG.length,manuals:V31_MANUAL_CATALOG.length,spells:V31_SPELL_CATALOG.length,equipment:V32_EQUIPMENT_CATALOG.length,artifacts:V32_EQUIPMENT_CATALOG.filter(x=>x.trueArtifact).length,enemies:ENEMIES.length,auctionPool:V35_AUCTION_POOL.length,spaceRegions:Object.keys(V36_HIGH_REGIONS)}}
function v36StateSnapshot(){ensureV36VoidShape();const p=state.player;return {spaceInsight:p.v36SpaceInsight,voidBacklash:p.v36VoidBacklash,refiningFailures:p.v36RefiningFailures,spaceActions:p.v36SpaceActions,spaceSkillUses:p.v36SpaceSkillUses,riftWins:p.v36RiftWins,voidEssence:v33MaterialCount('mat-v36-void-essence')}}
function v36SetPlayerForTest({realmIndex=null,location=null,spaceInsight=null,voidEssence=null,insight=null,deification=null,progressFull=false,injury=null,stones=null}={}){ensureV36VoidShape();const p=state.player;if(realmIndex!=null)p.realmIndex=clamp(Number(realmIndex)||0,0,REALMS.length-1);if(location)p.location=location;if(spaceInsight!=null)p.v36SpaceInsight=Math.max(0,Number(spaceInsight)||0);if(voidEssence!=null){p.voidEssence=Math.max(0,Number(voidEssence)||0);p.materialInventoryById['mat-v36-void-essence']=p.voidEssence}if(insight!=null)p.insight=Math.max(0,Number(insight)||0);if(deification!=null)p.deificationEssence=Math.max(0,Number(deification)||0);if(injury!=null)p.injury=clamp(Number(injury)||0,0,3);if(stones!=null)p.spiritStones=Math.max(0,Number(stones)||0);if(progressFull)p.progress=realm().need;p.hp=maxHp();p.qi=maxQi();syncV33AlchemyState();return true}
function renderV36VoidPanel(){let panel=$('v36VoidPanel');if(!panel){const page=$('page-character');if(!page)return;panel=document.createElement('section');panel.className='panel';panel.id='v36VoidPanel';panel.innerHTML='<h2>炼虚 · 空间之道</h2><p class="section-tip">化神之后不再只堆修为。炼虚要求你进入天渊边荒，积累空间感悟、凝炼炼虚真髓，并学会挪移、封锁与虚化。普通低阶地图无法直接产出这一阶段的核心资源。</p><div id="v36VoidInfo"></div><div id="v36VoidActions"></div>';page.appendChild(panel)}ensureV36VoidShape();const p=state.player,info=$('v36VoidInfo'),actions=$('v36VoidActions');if(!info||!actions)return;const meta=V36_HIGH_REGIONS[p.location],req=p.realmIndex===25?majorBreakthroughRequirements():null;info.innerHTML='<div class="kv"><span>空间感悟</span><b>'+p.v36SpaceInsight+' / 120</b></div><div class="kv"><span>炼虚真髓</span><b>'+v33MaterialCount('mat-v36-void-essence')+'</b></div><div class="kv"><span>虚空反噬</span><b>'+p.v36VoidBacklash+' 次</b></div><div class="kv"><span>高阶空间实战</span><b>'+p.v36RiftWins+' 胜</b></div>'+(req?'<div class="section-tip">炼虚破界：无伤 · 化神道种 '+req.deification+' · 悟道 '+req.insight+' · 炼虚真髓 '+req.voidEssence+' · 空间感悟 '+req.spaceInsight+'。</div>':'');let html='';if(meta)html+='<div class="skill-card"><div class="skill-head"><b>'+esc(p.location)+'</b><span>空间层级 '+meta.tier+'</span></div><div class="skill-desc">可闭关 '+meta.days+' 日观测空间褶皱；越危险的区域感悟更快，但反噬风险也更高。</div><div class="event-actions"><button data-v36-space>参悟空间 · '+meta.days+'日</button>'+(p.realmIndex>=25?'<button data-v36-essence>凝炼炼虚真髓 · 5日</button>':'')+'</div></div>';else html='<div class="section-tip">前往古河遗迹或玄阴禁地，可寻找通往【天渊城】的高阶道路。</div>';actions.innerHTML=html;const a=actions.querySelector('[data-v36-space]');if(a)a.onclick=()=>{const r=v36ContemplateSpace();showResult(r.ok?'空间感悟':'无法参悟',r.ok?'空间感悟 +'+r.gain+(r.backlash?'，但遭遇虚空反噬。':''):'原因：'+r.reason,r.ok&&!r.backlash?'good':'bad')};const e=actions.querySelector('[data-v36-essence]');if(e)e.onclick=()=>{const r=v36CraftVoidEssence();showResult(r.ok?'炼虚真髓凝成':'无法凝炼',r.ok?'炼虚真髓 +1':'原因：'+r.reason,r.ok?'good':'bad')}}
