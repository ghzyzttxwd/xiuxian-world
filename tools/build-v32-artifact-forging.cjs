const fs=require('fs');
const crypto=require('crypto');
const INPUT='src/game-v31.js',OUTPUT='src/game-v32.js',CATALOG='content/v32-equipment.cjs',RUNTIME='tools/v32-artifact-runtime.txt',REPORT='BUILD_V32_ARTIFACT_FORGING.json',BUILD='3201';
for(const f of [INPUT,CATALOG,RUNTIME])if(!fs.existsSync(f))throw new Error('V3.2 build missing '+f);
const equipment=require('../'+CATALOG);if(equipment.length!==60)throw new Error('V3.2 equipment count mismatch');if(equipment.filter(x=>x.trueArtifact).length!==24)throw new Error('V3.2 artifact count mismatch');
let src=fs.readFileSync(INPUT,'utf8');let runtime=fs.readFileSync(RUNTIME,'utf8').trimEnd().replace('__EQUIPMENT_DATA__',JSON.stringify(equipment));
function must(search,replacement,label){if(!src.includes(search))throw new Error('V3.2 transform did not match: '+label);src=src.replace(search,replacement)}
function replaceBetween(start,end,replacement,label){const a=src.indexOf(start);if(a<0)throw new Error('V3.2 start anchor missing: '+label);const b=src.indexOf(end,a+start.length);if(b<0)throw new Error('V3.2 end anchor missing: '+label);src=src.slice(0,a)+replacement+src.slice(b)}

must("const SAVE_KEY='xiuxian_world_v02'; const OLD_KEY='xiuxian_world_v01'; const VERSION='3.1.0'; const SAVE_SCHEMA_VERSION=28;","const SAVE_KEY='xiuxian_world_v02'; const OLD_KEY='xiuxian_world_v01'; const VERSION='3.2.0'; const SAVE_SCHEMA_VERSION=29;",'version/schema');
must('const CONTENT_STATE_VERSION=2;','const CONTENT_STATE_VERSION=3;','registry version');

replaceBetween('function itemRegistry(){','\nfunction artifactRegistry(){',`function itemRegistry(){const out={};for(const row of V32_EQUIPMENT_CATALOG)out[row.itemId]={id:row.itemId,itemId:row.itemId,name:row.name,type:row.kind,slot:row.slot,artifactSlot:row.artifactSlot,artifactId:row.artifactId,qualityId:row.qualityId,tier:row.tier,realmRequirement:realmIdFromIndex(row.unlock),path:row.path,sources:[...row.sources],cost:JSON.parse(JSON.stringify(row.cost)),stats:JSON.parse(JSON.stringify(row.stats)),passive:JSON.parse(JSON.stringify(row.passive||{})),active:row.active?JSON.parse(JSON.stringify(row.active)):null,legacyKey:row.legacyKey||null};for(const d of Object.values(CONSUMABLE_ITEM_META))out[d.id]={...d,itemId:d.id,type:'consumable'};return out}`,'item registry');
replaceBetween('function artifactRegistry(){','\nfunction recipeRegistry(){',`function artifactRegistry(){const out={};for(const row of V32_EQUIPMENT_CATALOG.filter(x=>x.trueArtifact))out[row.artifactId]={id:row.artifactId,artifactId:row.artifactId,itemId:row.itemId,name:row.name,type:'artifact',artifactSlot:row.artifactSlot,qualityId:row.qualityId,tier:row.tier,realmRequirement:realmIdFromIndex(row.unlock),path:row.path,sources:[...row.sources],activeAbility:row.active?JSON.parse(JSON.stringify(row.active)):null,passiveEffects:JSON.parse(JSON.stringify(row.passive||{})),refinement:{supported:true,maxLayer:9},binding:{supported:true,birthArtifact:true},warmth:{supported:true,max:100},damageState:{supported:true,repairable:true}};return out}`,'artifact registry');

replaceBetween('function syncStableContentState(){','\nfunction ensureContentStateShape(){',`function syncStableContentState(){if(!state?.player)return false;const p=state.player;p.contentStateVersion=CONTENT_STATE_VERSION;p.realmId=realmIdFromIndex(p.realmIndex);p.regionId=regionIdFromName(p.location);p.manualId=manualIdFromName(p.manual);p.spellProficiencyById={};for(const [name,prof] of Object.entries(p.spells||{})){const id=spellIdFromName(name);if(id)p.spellProficiencyById[id]=Number(prof)||0}p.materialCountsById={};for(const d of Object.values(MATERIAL_REGISTRY))p.materialCountsById[d.id]=Math.max(0,Number(p[d.field])||0);p.itemCountsById={};for(const d of Object.values(CONSUMABLE_ITEM_META))p.itemCountsById[d.id]=Math.max(0,Number(p[d.field])||0);if(!Array.isArray(p.gearOwnedIds)||!p.gearOwnedIds.length)p.gearOwnedIds=(Array.isArray(p.gearOwned)?p.gearOwned:[]).map(gearItemIdFromKey).filter(Boolean);if(!p.equippedItemIds||typeof p.equippedItemIds!=='object'||Array.isArray(p.equippedItemIds)){const eq=p.equipped||{};p.equippedItemIds={weapon:gearItemIdFromKey(eq.weapon),armor:gearItemIdFromKey(eq.armor),charm:gearItemIdFromKey(eq.charm)}}if(!Array.isArray(p.artifactOwnedIds))p.artifactOwnedIds=(Array.isArray(p.gearOwned)?p.gearOwned:[]).map(artifactIdFromGearKey).filter(Boolean);return true}`,'stable gear preservation');

must(" 28(){ensureV31GrowthShape()} \n};"," 28(){ensureV31GrowthShape()},\n 29(){ensureV32GearShape()} \n};",'schema29 migration');
must('state.player.rootIndex=chooseRoot();state.npcs=createNPCs();ensureContentStateShape();ensureV31GrowthShape();ensureNpcLifeShape();','state.player.rootIndex=chooseRoot();state.npcs=createNPCs();ensureContentStateShape();ensureV31GrowthShape();ensureV32GearShape();ensureNpcLifeShape();','new state v32');
must("['player.skillLoadoutVersion',p.skillLoadoutVersion],","['player.skillLoadoutVersion',p.skillLoadoutVersion],['player.equipmentInventory',p.equipmentInventory],['player.artifactLoadout',p.artifactLoadout],['player.forgingProf',p.forgingProf],['player.natalArtifactId',p.natalArtifactId===null?'none':p.natalArtifactId],",'schema v32 fields');
must('function save(){try{if(!state)return false;syncV31GrowthState();syncStableContentState();state.version=VERSION;','function save(){try{if(!state)return false;syncV31GrowthState();syncV32GearState();syncStableContentState();syncV32GearState();state.version=VERSION;','save v32 sync');

replaceBetween('const GEAR_ITEMS={','\n\n\nconst DWELLINGS=[',runtime+'\n\n','replace complete gear system');
must("allyCounted:false,cooldowns:{},statuses:{},v31Shield:0","allyCounted:false,cooldowns:{},statuses:{},artifactCooldowns:{},v31Shield:0",'artifact combat cooldown container');
must("*v31SkillDamageMultiplier(def);","*v31SkillDamageMultiplier(def)*v32EquippedDamageMultiplier(def);",'artifact skill synergy');
must("*daoIncomingMultiplier()*v31IncomingMultiplier()));","*daoIncomingMultiplier()*v31IncomingMultiplier()*v32ArtifactIncomingMultiplier()));",'artifact incoming passive');
must("${v31CombatSkillButtons()}<button data-combat=\"defend\">","${v31CombatSkillButtons()}${v32ArtifactCombatButtons()}<button data-combat=\"defend\">",'artifact combat buttons');
must("function combatAction(a){if(!combat||state.flags.dead)return;const e=combat.enemy;combat.defending=false;if(String(a).startsWith('skill:')){if(!useV31CombatSkill(String(a).slice(6),e))return}else if(a==='attack'){","function combatAction(a){if(!combat||state.flags.dead)return;const e=combat.enemy;combat.defending=false;if(String(a).startsWith('artifact:')){if(!useV32ArtifactActive(String(a).slice(9),e))return}else if(String(a).startsWith('skill:')){if(!useV31CombatSkill(String(a).slice(6),e))return}else if(a==='attack'){",'artifact combat action');
must('tickSkillCooldowns();tickCombatStatuses()}','tickSkillCooldowns();tickCombatStatuses();v32AfterCombatRound()}','artifact cooldown tick');
must('window.__TAIXUAN_TEST__={contentRegistrySnapshot,ensureContentStateShape,syncStableContentState,ensureV31GrowthShape,','window.__TAIXUAN_TEST__={contentRegistrySnapshot,ensureContentStateShape,syncStableContentState,ensureV31GrowthShape,ensureV32GearShape,syncV32GearState,v32CatalogSnapshot,forgeV32Item,bindV32Artifact,equipV32Artifact,refineV32Artifact,repairV32Artifact,warmV32Artifact,makeNatalV32Artifact,useV32ArtifactActive,v32ArtifactIncomingMultiplier,v32EquippedDamageMultiplier,','test api v32');

fs.writeFileSync(OUTPUT,src,'utf8');const sha=crypto.createHash('sha256').update(src).digest('hex');
const report={status:'PASS',gameplay_version:'3.2.0',build:BUILD,milestone:'artifact-forging-system',source:OUTPUT,source_sha256:sha,source_bytes:Buffer.byteLength(src),save_schema_version:29,content_registry_version:3,counts:{equipment:equipment.length,true_artifacts:equipment.filter(x=>x.trueArtifact).length,active_artifacts:equipment.filter(x=>x.trueArtifact&&x.active).length,build_passive_artifacts:equipment.filter(x=>x.trueArtifact&&x.buildPassive).length,base_slots:3,artifact_slots:4,manuals:28,spells:60,enemies:29,regions:12},checks:['60 differentiated equipment and artifacts','24 true artifacts','at least 12 active artifact abilities','at least 12 build-level artifact passives','three base equipment slots and four artifact slots','forging success chance and quality fluctuation','independent forging proficiency','artifact binding refinement warmth and natal conversion','major refinement failure can damage artifacts','repairable artifact damage without per-hit durability','artifact active cooldowns integrated into combat','path and manual artifact synergy','schema28 to schema29 migration','legacy seven gear preserved','V3.1 manual spell system preserved','future schema30 protection preserved']};
fs.writeFileSync(REPORT,JSON.stringify(report,null,2)+'\n');console.log('V32_BUILD_PASS',JSON.stringify(report));
