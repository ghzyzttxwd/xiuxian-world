const fs=require('fs');
const crypto=require('crypto');

const INPUT='src/game-v30.js';
const OUTPUT='src/game-v31.js';
const MANUALS_JSON='content/v31-manuals.json';
const SPELLS_JSON='content/v31-spells.json';
const RUNTIME='tools/v31-manual-spell-runtime.txt';
const REPORT='BUILD_V31_MANUAL_SPELL_EXPANSION.json';
const BUILD='3101';
for(const f of [INPUT,MANUALS_JSON,SPELLS_JSON,RUNTIME])if(!fs.existsSync(f))throw new Error('V3.1 build missing '+f);
const manuals=JSON.parse(fs.readFileSync(MANUALS_JSON,'utf8'));
const spells=JSON.parse(fs.readFileSync(SPELLS_JSON,'utf8'));
if(manuals.length!==28)throw new Error('V3.1 manual catalog must contain 28 entries, got '+manuals.length);
if(spells.length!==60)throw new Error('V3.1 spell catalog must contain 60 entries, got '+spells.length);
for(const [label,rows] of [['manual',manuals],['spell',spells]]){const ids=rows.map(x=>x.id),names=rows.map(x=>x.name);if(new Set(ids).size!==ids.length)throw new Error('duplicate '+label+' id');if(new Set(names).size!==names.length)throw new Error('duplicate '+label+' name')}
let src=fs.readFileSync(INPUT,'utf8');
let runtime=fs.readFileSync(RUNTIME,'utf8').trimEnd().replace('__MANUAL_DATA__',JSON.stringify(manuals)).replace('__SPELL_DATA__',JSON.stringify(spells));
function must(search,replacement,label){if(!src.includes(search))throw new Error('V3.1 transform did not match: '+label);src=src.replace(search,replacement)}
function insertBefore(search,text,label){const at=src.indexOf(search);if(at<0)throw new Error('V3.1 insert did not match: '+label);src=src.slice(0,at)+text+src.slice(at)}
function replaceBetween(start,end,replacement,label){const a=src.indexOf(start);if(a<0)throw new Error('V3.1 start anchor missing: '+label);const b=src.indexOf(end,a+start.length);if(b<0)throw new Error('V3.1 end anchor missing: '+label);src=src.slice(0,a)+replacement+src.slice(b)}

must("const SAVE_KEY='xiuxian_world_v02'; const OLD_KEY='xiuxian_world_v01'; const VERSION='3.0.0'; const SAVE_SCHEMA_VERSION=27;","const SAVE_KEY='xiuxian_world_v02'; const OLD_KEY='xiuxian_world_v01'; const VERSION='3.1.0'; const SAVE_SCHEMA_VERSION=28;",'version/schema');
must('const CONTENT_STATE_VERSION=1;','const CONTENT_STATE_VERSION=2;','content registry version');

must("function manualIdFromName(name){return MANUAL_ID_BY_NAME[name]||null}","function manualIdFromName(name){return V31_MANUAL_ID_BY_NAME[name]||MANUAL_ID_BY_NAME[name]||null}",'manual stable ids');
must("function spellIdFromName(name){return SPELL_ID_BY_NAME[name]||null}","function spellIdFromName(name){return V31_SPELL_ID_BY_NAME[name]||SPELL_ID_BY_NAME[name]||null}",'spell stable ids');
replaceBetween('function manualRegistry(){','\nfunction spellRegistry(){',`function manualRegistry(){const out={};for(const [name,d] of Object.entries(MANUALS)){const id=manualIdFromName(name);if(!id)continue;const meta=v31ManualCatalogRow(id)||{};out[id]={id,manualId:id,name,type:'manual',qualityId:qualityIdFromLabel(d.quality),qualityLabel:d.quality,mult:d.mult,desc:d.desc,unlock:Number(meta.unlock)||0,realmRequirement:realmIdFromIndex(meta.unlock||0),path:meta.path||'none',sources:[...(meta.sources||[])],cost:v31DefaultCost(meta,'manual'),effects:JSON.parse(JSON.stringify(meta.effects||{}))}}return out}`,'manual registry');
replaceBetween('function spellRegistry(){','\nfunction realmRegistry(){',`function spellRegistry(){const out={};for(const [name,d] of Object.entries(SPELLS)){const id=spellIdFromName(name);if(!id)continue;const meta=v31SpellCatalogRow(id)||{},old=SPELL_FOUNDATION_META[id]||{};out[id]={id,spellId:id,name,type:'spell',qualityId:qualityIdFromLabel(d.quality),qualityLabel:d.quality,qi:d.qi||0,power:d.power||0,desc:d.desc,category:meta.category||old.category||'utility',cooldown:Number(meta.cooldown??old.cooldown)||0,statusIds:[...(meta.statusIds||old.statusIds||[])],path:meta.path||old.path||'none',unlock:Number(meta.unlock)||0,realmRequirement:realmIdFromIndex(meta.unlock||0),sources:[...(meta.sources||[])],cost:v31DefaultCost(meta,'spell'),effect:JSON.parse(JSON.stringify(meta.effect||{}))}}return out}`,'spell registry');
replaceBetween('function applyCombatStatus(statusId,duration=null,sourceId=null){','\nfunction combatStatusTurns(statusId){',`function applyCombatStatus(statusId,duration=null,sourceId=null){const c=ensureCombatFoundation(),d=v31AllStatusRegistry()[statusId];if(!c||!d)return null;const turns=Math.max(1,Number(duration==null?d.defaultDuration:duration)||1),old=c.statuses[statusId];c.statuses[statusId]={id:statusId,turns:d.stack==='stack'?(old?.turns||0)+turns:Math.max(old?.turns||0,turns),sourceId:sourceId||null};return {...c.statuses[statusId]}}`,'status registry extension');
replaceBetween('function contentRegistrySnapshot(){','\nfunction rand(){',`function contentRegistrySnapshot(){const manuals=manualRegistry(),spells=spellRegistry(),items=itemRegistry(),artifacts=artifactRegistry(),materials=MATERIAL_REGISTRY,recipes=recipeRegistry(),drops=dropTableRegistry(),enemies=enemyRegistry(),realms=realmRegistry(),regions=regionRegistry(),statuses=v31AllStatusRegistry();return {registryVersion:CONTENT_STATE_VERSION,quality:JSON.parse(JSON.stringify(QUALITY_TIERS)),manuals,spells,items,artifacts,materials:JSON.parse(JSON.stringify(materials)),recipes,drops,enemies,realms,regions,statuses:JSON.parse(JSON.stringify(statuses)),counts:{manuals:Object.keys(manuals).length,spells:Object.keys(spells).length,items:Object.keys(items).length,artifacts:Object.keys(artifacts).length,materials:Object.keys(materials).length,recipes:Object.keys(recipes).length,drops:Object.keys(drops).length,enemies:Object.keys(enemies).length,realms:Object.keys(realms).length,regions:Object.keys(regions).length,statuses:Object.keys(statuses).length}}}\n\n${runtime}\n`,'content registry and V3.1 runtime');

must(" 27(){ensureContentStateShape()} \n};"," 27(){ensureContentStateShape()},\n 28(){ensureV31GrowthShape()} \n};",'schema 28 migration');
must('state.player.rootIndex=chooseRoot();state.npcs=createNPCs();ensureContentStateShape();ensureNpcLifeShape();','state.player.rootIndex=chooseRoot();state.npcs=createNPCs();ensureContentStateShape();ensureV31GrowthShape();ensureNpcLifeShape();','new state V3.1 shape');
must("['player.artifactOwnedIds',p.artifactOwnedIds],","['player.artifactOwnedIds',p.artifactOwnedIds],['player.manualLibraryIds',p.manualLibraryIds],['player.manualProficiencyById',p.manualProficiencyById],['player.activeSkillIds',p.activeSkillIds],['player.passiveSkillId',p.passiveSkillId===null?'none':p.passiveSkillId],['player.skillLoadoutVersion',p.skillLoadoutVersion],",'schema validation V3.1 fields');
must('function save(){try{if(!state)return false;syncStableContentState();state.version=VERSION;','function save(){try{if(!state)return false;syncV31GrowthState();syncStableContentState();state.version=VERSION;','save V3.1 sync');

must("function maxHp(){return Math.max(1,Math.floor((realm().maxHp+gearHp())*daoHpMultiplier()))} function maxQi(){return Math.max(0,Math.floor((realm().maxQi+gearQi())*daoQiMultiplier()))}","function maxHp(){return Math.max(1,Math.floor((realm().maxHp+gearHp())*daoHpMultiplier()*v31ManualHpMultiplier()*v31PassiveHpMultiplier()))} function maxQi(){return Math.max(0,Math.floor((realm().maxQi+gearQi())*daoQiMultiplier()*v31ManualQiMultiplier()*v31PassiveQiMultiplier()))}",'manual/passive max stats');
replaceBetween('function cultivate(){','\n\nfunction realmLifespanFloor',`function cultivate(){\n if(livingActionGuard())return;const gain=cultivationGainForDays(1),prof=rint(4,7);const before=state.player.progress;state.player.progress=Math.min(realm().need,state.player.progress+gain);state.player.manualProf+=prof;addDiligence(1);advanceDays(1);if(state.flags.dead)return;applyV31ManualCultivationAftermath();const actual=state.player.progress-before,full=state.player.progress>=realm().need;addPersonal('你运转《'+state.player.manual+'》吐纳一日，修为 +'+Math.max(0,actual)+'，功法熟练度 +'+prof+'。'+(full?'当前境界已经圆满，可主动尝试破境。':''),full?'major':'good');save();render();showResult('吐纳修炼','修为 +'+Math.max(0,actual)+'\\n《'+state.player.manual+'》熟练度 +'+prof+(full?'\\n境界圆满：现在可以尝试突破。':''),'good')\n}`,'cultivation manual side effects');
replaceBetween('function breakthroughChance(){','\nfunction attemptBreakthrough(){',`function breakthroughChance(){\n if(state.player.realmIndex>=REALMS.length-1)return 0;const req=majorBreakthroughRequirements();let base=req?req.base:minorBreakthroughBase(state.player.realmIndex);const rootBonus=(root().mult-1)*.18,manualBonus=Math.min(.12,(state.player.manualProf||0)/1800)+v31BreakthroughBonus(),injuryPenalty=(state.player.injury||0)*.08,pity=(state.player.breakthroughPity||0)/100;return clamp(base+rootBonus+manualBonus+pity-injuryPenalty,req?.kind?.08:.16,req?.kind?.82:.92)\n}`,'breakthrough manual bonus');

must("allyCounted:false,cooldowns:{},statuses:{},logs:","allyCounted:false,cooldowns:{},statuses:{},v31Shield:0,v31Control:0,v31Counter:0,v31Vulnerable:0,v31Regen:0,v31Rage:0,logs:",'combat V3.1 fields');
replaceBetween('function renderCombat(){','\nfunction combatAction(a){',`function renderCombat(){const e=combat.enemy,pPct=clamp(combat.playerHp/maxHp()*100,0,100),ePct=clamp(combat.enemyHp/e.hp*100,0,100),passive=v31PassiveRow(),stateText=v31CombatStateText();let html=\`<h2>遭遇战 · 第\${combat.round}回合</h2><div class="combatants"><div class="fighter"><b>\${esc(state.player.name)}</b><div class="hp">\${realm().name} · 气血 \${combat.playerHp}/\${maxHp()} · 灵力 \${combat.playerQi}/\${maxQi()}</div><div class="bar"><i style="width:\${pPct}%"></i></div></div><div class="vs">VS</div><div class="fighter"><b>\${esc(e.name)}</b><div class="hp">\${esc(e.kind||'敌人')} · \${REALMS[Math.min(e.realm||0,REALMS.length-1)].name} · \${dangerLabel(e)}</div><div class="hp">气血 \${Math.max(0,combat.enemyHp)}/\${e.hp}</div><div class="bar"><i style="width:\${ePct}%"></i></div></div></div><div class="section-tip">六格主动栏\${passive?' · 被动：'+esc(passive.name):''}\${stateText?' · '+esc(stateText):''}</div><div class="combat-log">\${combat.logs.slice(-10).map(x=>\`• \${esc(x)}<br>\`).join('')}</div><div class="modal-actions two"><button data-combat="attack">拳脚攻击</button>\${v31CombatSkillButtons()}<button data-combat="defend">防御</button><button data-combat="flee">逃跑</button></div>\`;openModal(html,false);document.querySelectorAll('[data-combat]').forEach(b=>b.onclick=()=>combatAction(b.dataset.combat))}`,'V3.1 combat UI');
must("function combatAction(a){if(!combat||state.flags.dead)return;const e=combat.enemy;combat.defending=false;if(a==='attack'){","function combatAction(a){if(!combat||state.flags.dead)return;const e=combat.enemy;combat.defending=false;if(String(a).startsWith('skill:')){if(!useV31CombatSkill(String(a).slice(6),e))return}else if(a==='attack'){",'generic skill action');
must('let dodged=false;if(combat.evade>0){','let dodged=false;if((combat.v31Control||0)>0){combat.v31Control--;dodged=true;combat.logs.push(e.name+\'受到封锁，本回合无法反击。\')}if(!dodged&&combat.evade>0){','control skip retaliation');
must('edmg=Math.max(1,Math.floor(edmg*realmSuppressionMultiplier(e.realm||0,state.player.realmIndex)*daoIncomingMultiplier()));','edmg=Math.max(1,Math.floor(edmg*realmSuppressionMultiplier(e.realm||0,state.player.realmIndex)*daoIncomingMultiplier()*v31IncomingMultiplier()));','manual/passive incoming multiplier');
must('combat.playerHp-=edmg;combat.logs.push(`${e.name}反击，造成 ${edmg} 点伤害。`)','if((combat.v31Shield||0)>0&&edmg>0){const blocked=Math.min(combat.v31Shield,edmg);combat.v31Shield-=blocked;edmg-=blocked;combat.logs.push(`护盾抵消 ${blocked} 点伤害。`)}if(edmg>0)combat.playerHp-=edmg;if(edmg>0&&(combat.v31Counter||0)>0){const reflected=Math.max(1,Math.floor(edmg*combat.v31Counter));combat.enemyHp-=reflected;combat.logs.push(`反震罡劲返还 ${reflected} 点伤害。`);combat.v31Counter=0}combat.logs.push(`${e.name}反击，造成 ${edmg} 点伤害。`)','shield/counter retaliation');
must('if(combat.playerHp<=0){finishCombat(false);return}combat.round++;renderCombat()}','if(combat.playerHp<=0){finishCombat(false);return}if(combat.enemyHp<=0){finishCombat(true);return}v31AfterCombatRound();combat.round++;renderCombat()}','round cooldown/status tick');

must('renderCorePath();renderDaoPath();renderCharacter();renderSect();','renderCorePath();renderDaoPath();renderCharacter();renderV31GrowthPanel();renderSect();','render V3.1 panel');
must('window.__TAIXUAN_TEST__={contentRegistrySnapshot,ensureContentStateShape,syncStableContentState,','window.__TAIXUAN_TEST__={contentRegistrySnapshot,ensureContentStateShape,syncStableContentState,ensureV31GrowthShape,syncV31GrowthState,v31CatalogSnapshot,learnV31Manual,switchV31Manual,learnV31Spell,equipV31Skill,equipV31Passive,v31CurrentManualEffects,v31BreakthroughBonus,v31IncomingMultiplier,v31HealingMultiplier,useV31CombatSkill,','test API V3.1');

fs.writeFileSync(OUTPUT,src,'utf8');
const sha=crypto.createHash('sha256').update(src).digest('hex');
const report={status:'PASS',gameplay_version:'3.1.0',build:BUILD,milestone:'manual-spell-expansion',source:OUTPUT,source_sha256:sha,source_bytes:Buffer.byteLength(src),save_schema_version:28,content_registry_version:2,counts:{manuals:manuals.length,spells:spells.length,passive_spells:spells.filter(x=>x.category==='passive').length,items:11,artifacts:4,materials:8,recipes:4,enemies:29,drop_tables:29,realms:26,regions:12,statuses:14,active_slots:6,passive_slots:1},checks:['28 manuals with stable IDs and differentiated effects','60 spells/divine arts with stable IDs','independent manual proficiency library','six active skill slots plus one passive slot','realm/location/path acquisition gates and real resource costs','generic combat skills use cooldowns and status engine','shield healing qi restore control dot counter execute drain and laststand mechanics','manual HP QI breakthrough damage healing incoming and artifact synergies','schema27 to schema28 migration','V3.0 and V2.9 compatibility retained','future schema29 protection preserved']};
fs.writeFileSync(REPORT,JSON.stringify(report,null,2)+'\n');
console.log('V31_BUILD_PASS',JSON.stringify(report));
