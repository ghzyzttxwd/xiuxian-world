name: Phase8 R9 Postfix Long Audit

on:
  push:
    branches: ["phase8-r9-postfix-audit"]
    paths: ["PHASE8_R9_POSTFIX.trigger"]

permissions:
  contents: write

concurrency:
  group: taixuan-phase8-r9-postfix-audit
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase8-r9-postfix-audit
          fetch-depth: 0

      - name: Verify R8 hotfix baseline
        shell: bash
        run: |
          set -euo pipefail
          EXPECT='1dc5860e93aab32d9dd14fe6fda1d34bd5004ab97198f57c73231a0bb5d5756c'
          test "$(sha256sum src/game-v39.js | awk '{print $1}')" = "$EXPECT"
          grep -q 'if(!pool.length)return null' src/game-v39.js
          grep -q "build:'3903'" app.js
          node -e "const x=require('./PHASE8_R8_VALIDATION.json');if(x.status!=='PASS'||x.core_build!==3903||x.rules.emptyEligiblePool!=='no-combat'||x.save_schema_version!==36)process.exit(1)"

      - name: Rebuild identical safe-human audit and run six fixed seeds
        shell: bash
        run: |
          set -euo pipefail
          npm init -y >/dev/null 2>&1
          npm install --no-save jsdom@24 >/dev/null 2>&1
          git fetch origin phase8-round3-safe-audit
          git show origin/phase8-round3-safe-audit:tests/playtest-phase8-round2.mjs > /tmp/playtest-phase8-r9.mjs
          cp /tmp/playtest-phase8-r9.mjs tests-playtest-phase8-r9.mjs
          python3 - <<'PY'
          from pathlib import Path
          p=Path('tests-playtest-phase8-r9.mjs')
          s=p.read_text()
          old="function settleCombat(api){let guard=0;while(api.getCombat?.()&&guard++<40){const c=api.getCombat(),s=api.getState();const ratio=(Number(c.playerHp)||0)/Math.max(1,hpMax(api,s));api.combatAction(ratio<.34?'defend':'attack')}return guard}"
          new="function settleCombat(api){let guard=0;while(api.getCombat?.()&&guard++<40){const c=api.getCombat(),s=api.getState();const ratio=(Number(c.playerHp)||0)/Math.max(1,hpMax(api,s)),diff=(Number(c.enemy?.realm)||0)-(Number(s.player.realmIndex)||0);if(diff>=1||ratio<.62)api.combatAction('flee');else if(ratio<.42)api.combatAction('defend');else api.combatAction('attack')}return guard}"
          if old not in s: raise SystemExit('safe-combat patch baseline missing')
          s=s.replace(old,new,1)
          s=s.replace("const OUT='PHASE8_ROUND2_REPORT.json';","const OUT='PHASE8_R9_POSTFIX_REPORT.json';",1)
          s=s.replace('maxActions:560','maxActions:800')
          s=s.replace("phase:'8-round2-human-navigation'","phase:'8-round9-postfix-navigation'",1)
          s=s.replace("dead:!!end.flags?.dead,milestones","dead:!!end.flags?.dead,deathCause:end.flags?.deathCause||null,milestones",1)
          p.write_text(s)
          PY
          node tests-playtest-phase8-r9.mjs
          test -s PHASE8_R9_POSTFIX_REPORT.json
          node - <<'NODE'
          const fs=require('fs');
          const p='PHASE8_R9_POSTFIX_REPORT.json';const x=JSON.parse(fs.readFileSync(p,'utf8'));
          x.coreBuild=3903;
          x.sourceSha256='1dc5860e93aab32d9dd14fe6fda1d34bd5004ab97198f57c73231a0bb5d5756c';
          x.comparisonBaseline={phase:'8-round6-postfix-navigation',runs:6,deaths:5,reached:{qi1:6,qi4:6,qi7:2,qi9:0,foundation:0},medianDays:{qi1:31,qi4:200,qi7:623,qi9:null,foundation:null}};
          const old=x.comparisonBaseline;
          x.delta={deaths:x.summary.deaths-old.deaths,reachedQi7:x.summary.reached.qi7-old.reached.qi7,reachedQi9:x.summary.reached.qi9-old.reached.qi9,reachedFoundation:x.summary.reached.foundation-old.reached.foundation};
          fs.writeFileSync(p,JSON.stringify(x,null,2)+'\n');
          console.log('PHASE8_R9_POSTFIX',JSON.stringify({summary:x.summary,flags:x.flags,delta:x.delta}));
          NODE

      - name: Validate report comparability
        shell: bash
        run: |
          set -euo pipefail
          node - <<'NODE'
          const x=require('./PHASE8_R9_POSTFIX_REPORT.json');
          if(x.phase!=='8-round9-postfix-navigation')throw Error('phase mismatch');
          if(x.coreBuild!==3903)throw Error('build mismatch');
          if(x.sourceSha256!=='1dc5860e93aab32d9dd14fe6fda1d34bd5004ab97198f57c73231a0bb5d5756c')throw Error('sha mismatch');
          if(x.summary.runs!==6)throw Error('run count mismatch');
          if(x.comparisonBaseline?.deaths!==5)throw Error('baseline mismatch');
          if(x.summary.travelRuns<5)throw Error('travel strategy not comparable');
          if(x.summary.manualUpgradeRuns<5)throw Error('manual strategy not comparable');
          console.log('R9_COMPARABILITY_OK',JSON.stringify(x.delta));
          NODE

      - name: Commit R9 report
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add PHASE8_R9_POSTFIX_REPORT.json
          git commit -m 'test: record phase8 r9 post-r8 long audit [skip ci]'
          git push origin HEAD:phase8-r9-postfix-audit
