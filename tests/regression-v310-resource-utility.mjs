import fs from 'fs';
import assert from 'assert';
import {JSDOM} from 'jsdom';

const html=fs.readFileSync('index.html','utf8').replace(/<script[^>]+src=["'][^"']*app\.js[^"']*["'][^>]*><\/script>/i,'');
const source=fs.readFileSync('src/game-v310.js','utf8');
const dom=new JSDOM(html,{url:'http://v310-resource-utility.test/',runScripts:'outside-only',pretendToBeVisual:true});
dom.window.matchMedia=()=>({matches:false,addListener(){},removeListener(){}});dom.window.scrollTo=()=>{};dom.window.console={...console,log(){},info(){},debug(){}};dom.window.eval(source);
const api=dom.window.__TAIXUAN_TEST__;assert(api,'V3.10 resource utility audit missing test API');api.newGame('V310资源用途审计');const reg=api.contentRegistrySnapshot();const materials=Object.values(reg.materials||{});const named=materials.filter(m=>m?.named===true);const namedIds=new Set(named.map(m=>m.id));const sinks=new Map(named.map(m=>[m.id,new Set()]));
function add(id,label){if(namedIds.has(id))sinks.get(id).add(label)}
function scanCostKeys(value,label,path='root'){if(Array.isArray(value)){value.forEach((v,i)=>scanCostKeys(v,label,`${path}[${i}]`));return}if(!value||typeof value!=='object')return;for(const [k,v] of Object.entries(value)){if(namedIds.has(k)&&Number.isFinite(Number(v))&&Number(v)>0)add(k,`${label}:${path}.${k}`);if(v&&typeof v==='object')scanCostKeys(v,label,`${path}.${k}`)}}
scanCostKeys(reg.recipes||{},'recipe-cost');scanCostKeys(reg.items||{},'item-cost');scanCostKeys(reg.artifacts||{},'artifact-cost');scanCostKeys(reg.formations||{},'formation-cost');
function scanSellable(value,path='shops'){if(Array.isArray(value)){value.forEach((v,i)=>scanSellable(v,`${path}[${i}]`));return}if(!value||typeof value!=='object')return;if(value.sellable===true&&typeof value.refId==='string')add(value.refId,`sellable:${path}`);for(const [k,v] of Object.entries(value))if(v&&typeof v==='object')scanSellable(v,`${path}.${k}`)}
scanSellable(reg.shops||{});
function frozenJsonCatalog(name,nextName){const startToken=`const ${name}=Object.freeze(`,endToken=`);\nconst ${nextName}=`;const start=source.indexOf(startToken);assert(start>=0,`${name} source catalog missing`);const bodyStart=start+startToken.length;const end=source.indexOf(endToken,bodyStart);assert(end>bodyStart,`${name} source catalog end missing`);return JSON.parse(source.slice(bodyStart,end))}
const fixedListings=frozenJsonCatalog('V35_FIXED_LISTINGS','V35_AUCTION_POOL');scanSellable(fixedListings,'fixed-listings');
const requestSelector="V33_MATERIAL_CATALOG.filter(m=>m.named&&(m.minRealm||0)<=Math.max(3,(n.realmIndex||0)+4)&&(m.locations||[]).includes(n.location))";const requestConsumer="v33AddMaterial(req.materialId,-req.cost)";assert(source.includes(requestSelector),'V3.10 named-material NPC request selector drifted');assert(source.includes(requestConsumer),'V3.10 named-material NPC request consumer drifted');
const homesStartToken='const NPC_FACTION_HOMES={',homesEndToken='};\nfunction npcSpawnLocation';const homesStart=source.indexOf(homesStartToken),homesEnd=source.indexOf(homesEndToken,homesStart+homesStartToken.length);assert(homesStart>=0&&homesEnd>homesStart,'NPC faction home catalog missing');const npcHomeBlock=source.slice(homesStart+homesStartToken.length,homesEnd);const npcReachableLocations=new Set([...npcHomeBlock.matchAll(/['"]([^'"]+)['"]/g)].map(m=>m[1]).filter(x=>Object.prototype.hasOwnProperty.call((api.getState?.()||{}),x)===false));const allMaterialLocations=new Set(named.flatMap(m=>Array.isArray(m.locations)?m.locations:[]));for(const x of [...npcReachableLocations])if(!allMaterialLocations.has(x))npcReachableLocations.delete(x);for(const m of named){const loc=(m.locations||[]).find(x=>npcReachableLocations.has(x));if(loc)add(m.id,`dynamic-npc-request:${loc}`)}
for(const line of source.split('\n')){if(!line.includes('function ')||!line.includes('v33AddMaterial'))continue;const fn=line.match(/function\s+([A-Za-z0-9_]+)/)?.[1]||'anonymous';for(const m of line.matchAll(/v33AddMaterial\(\s*['"](mat-[a-z0-9-]+)['"]\s*,\s*-\s*\d+/gi))add(m[1],`direct-consume:${fn}`);if(/v33AddMaterial\([^)]*,\s*-\s*[A-Za-z0-9_]+\s*\)/.test(line)){for(const m of line.matchAll(/['"](mat-[a-z0-9-]+)['"]\s*:\s*(\d+(?:\.\d+)?)/gi))if(Number(m[2])>0)add(m[1],`direct-cost-map:${fn}`)}}
const rows=named.map(m=>({id:m.id,name:m.name,minRealm:m.minRealm,kind:m.kind,sinks:[...sinks.get(m.id)].sort()}));const unused=rows.filter(r=>r.sinks.length===0);const high=rows.filter(r=>Number(r.minRealm)>=24);const highUnused=high.filter(r=>r.sinks.length===0);fs.writeFileSync('V310_RESOURCE_UTILITY_AUDIT.json',JSON.stringify({status:unused.length?'FAIL':'PASS',namedMaterials:rows.length,highTierMaterials:high.length,npcReachableLocations:[...npcReachableLocations].sort(),unused,highUnused,rows},null,2)+'\n');assert.strictEqual(highUnused.length,0,`V3.10 high-tier named materials without a gameplay/economic sink: ${highUnused.map(x=>`${x.id}(${x.name})`).join(', ')}`);assert.strictEqual(unused.length,0,`V3.10 named materials without a gameplay/economic sink: ${unused.map(x=>`${x.id}(${x.name})`).join(', ')}`);console.log('V310_RESOURCE_UTILITY_REGRESSION_PASS '+JSON.stringify({namedMaterials:rows.length,highTierMaterials:high.length,unused:0,highUnused:0,fixedShopSellablesAudited:true,dynamicNpcRequestsAudited:true,npcReachableLocations:[...npcReachableLocations].sort()}));
